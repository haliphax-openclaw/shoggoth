--- a/packages/platform-discord/test/discord-platform.test.ts
+++ b/packages/platform-discord/test/discord-platform.test.ts
@@ -298,7 +298,7 @@ describe("startDiscordPlatform", { concurrency: false }, () => {
     });
   }
 
-  afterEach(() => {
+  afterEach(async () => {
     closeTestDb(db, tmp);
-  });
+    await closeTestDb(db, tmp);
+  });
 
 
