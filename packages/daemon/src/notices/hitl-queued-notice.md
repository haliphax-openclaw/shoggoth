**HITL** — tool blocked pending human approval
id: `{{id}}`
session: `{{sessionId}}`
tool: `{{toolName}}` ({{riskTier}})
{{correlationLine}}{{payloadLine}}Reactions (owner): 1️⃣ approve once · ✅ this tool for session + auto · ♾️ this tool for agent + auto · ❌ deny.
CLI: `shoggoth hitl approve <id>` · `shoggoth hitl deny <id>` (or control ops `hitl_pending_approve` / `hitl_pending_deny`).
